5.3 WPF приложение (простая версия)

MainWindow.xaml

```xml
<Window x:Class="WpfApp.MainWindow"
        Title="Добавить кадр" Height="200" Width="300">
    <StackPanel Margin="10">
        <Label>Выберите фильм:</Label>
        <ComboBox x:Name="comboFilms"/>
        <Button Content="Добавить кадр" 
                Click="AddFrame_Click" 
                Margin="0,20,0,0"/>
    </StackPanel>
</Window>
```

MainWindow.xaml.cs

```csharp
using System;
using System.IO;
using System.Windows;
using Microsoft.Win32;

public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
        // Заполняем комбобокс (заглушка)
        comboFilms.Items.Add("Фильм 1");
        comboFilms.Items.Add("Фильм 2");
        comboFilms.SelectedIndex = 0;
    }

    private void AddFrame_Click(object sender, RoutedEventArgs e)
    {
        // 1. Открываем диалог выбора файла
        var dialog = new OpenFileDialog
        {
            Filter = "Картинки|*.jpg;*.png;*.bmp"
        };

        if (dialog.ShowDialog() == true)
        {
            string filePath = dialog.FileName;
            
            // 2. Проверяем размер (не более 2 МБ)
            long fileSize = new FileInfo(filePath).Length;
            if (fileSize > 2 * 1024 * 1024)
            {
                MessageBox.Show("Слишком большой файл! Макс. 2 МБ");
                return;
            }
            
            // 3. Копируем в папку images
            string exeDir = AppDomain.CurrentDomain.BaseDirectory;
            string imagesDir = Path.Combine(exeDir, "images");
            
            if (!Directory.Exists(imagesDir))
                Directory.CreateDirectory(imagesDir);
            
            string fileName = Path.GetFileName(filePath);
            string destPath = Path.Combine(imagesDir, fileName);
            
            try
            {
                File.Copy(filePath, destPath, true);
                MessageBox.Show($"Файл сохранен:\n{destPath}");
            }
            catch (Exception ex)
            {
                MessageBox.Show($"Ошибка: {ex.Message}");
            }
        }
    }
}
```

Что делает:

1. Выбираем фильм из списка (заглушка)
2. Нажимаем кнопку → открывается диалог выбора файла
3. Если файл > 2 МБ → ошибка
4. Файл копируется в папку images рядом с программой
5. Если папки нет → создается

Никакой БД, никаких сложностей.
